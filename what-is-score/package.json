{
  "name": "what-is-score",
  "private": true,
  "version": "0.1.0",
  "description": "Naver shopping ranking checker for Smart Store sellers",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env server.js",
    "cf:dev": "npx wrangler dev --port 8000",
    "cf:deploy": "npx wrangler deploy",
    "cf:dry-run": "npx wrangler deploy --dry-run"
  }
}
